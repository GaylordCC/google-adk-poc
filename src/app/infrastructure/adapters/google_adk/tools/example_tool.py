"""Example placeholder for a Google ADK tool.

GUIDE: a real tool would look like:

    async def get_something(...) -> ...:
        '''Docstring the LLM reads to decide when to call this tool.'''
        return await some_use_case.execute(...)

Registered in agent_factory.py when building the Agent. No tool logic
is implemented yet — this file exists only to mark where tools live.
"""
